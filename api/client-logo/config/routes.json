{
  "routes": [
    {
      "method": "GET",
      "path": "/client-logos",
      "handler": "client-logo.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/client-logos/count",
      "handler": "client-logo.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/client-logos/:id",
      "handler": "client-logo.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/client-logos",
      "handler": "client-logo.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/client-logos/:id",
      "handler": "client-logo.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/client-logos/:id",
      "handler": "client-logo.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
